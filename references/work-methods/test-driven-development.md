# Test-driven development

The worker writes a failing test for the specified behavior, makes the narrow code change that passes it, and records both the intended signal and the final command result.
