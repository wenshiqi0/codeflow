# Implementation followed by self-review

The worker completes the change, inspects the resulting diff for unrelated edits and missing boundaries, runs the applicable checks, and records remaining limitations.
