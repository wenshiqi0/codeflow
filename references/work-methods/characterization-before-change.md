# Characterization before change

The worker records current behavior with executable checks or bounded observations, then preserves or deliberately updates that behavior while changing the implementation.
