{
  "name": "codeflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Goal-scoped handoff and receipt runtime for equal coding workers",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json",
    "doctor": "./scripts/doctor.sh"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@types/bun": "latest",
    "typebox": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
